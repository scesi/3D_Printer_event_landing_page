<section class="section-cards">
   <article class="article-card">
      <header class="header-title">
         <h2 class="card-title">Inscríbete Aquí</h2>
      </header>

      <section class="section-content">
         <p class="text-center text-base md:text-xl">
            ¡Te esperamos para compartir, <br /> aprender y crear juntos!
         </p>
         <button class="button-card"> Inscribirse </button>
      </section>
   </article>

   <article class="article-card">
      <header class="header-title">
         <h2 class="card-title">Información del evento</h2>
      </header>

      <section class="section-content">
         <p class="text-center text-base md:text-xl">Convocatoria del evento</p>
         <button class="button-card"> Más información </button>
      </section>
   </article>
</section>

<style>
   /* Contenedor general de las tarjetas */
   .section-cards {
      @apply flex flex-col items-center gap-6 p-4 md:flex-row md:justify-between md:p-8;
   }

   /* Estructura de las tarjetas */
   .article-card {
      @apply flex w-full max-w-[580px] flex-1 flex-col justify-between rounded-2xl bg-white shadow-2xl transition-transform hover:scale-105 md:h-[300px];
      min-height: 0; /* Asegura que no se desborde */
   }

   /* Título de la tarjeta */
   .header-title {
      @apply flex h-24 items-center justify-center rounded-t-2xl bg-[#29A5F2] text-center text-[28px] font-semibold text-white;
   }

   .card-title {
      @apply mx-4 text-[20px] md:text-[28px];
   }

   /* Contenido de la tarjeta */
   .section-content {
      @apply flex flex-grow flex-col items-center justify-center gap-6 p-4;
   }

   /* Botones */
   .button-card {
      @apply mx-4 h-[54px] w-5/6 max-w-[360px] rounded-3xl bg-[#29A5F2] px-4 py-2 text-[18px] font-semibold text-white shadow-lg transition-colors hover:bg-[#6cb7e6] md:w-full;
   }

   /* Ajustes responsivos */
   @media (min-width: 768px) {
      .section-cards {
         @apply gap-8;
      }

      .article-card {
         @apply w-[580px];
      }

      .button-card {
         @apply text-[24px];
      }
   }
</style>
